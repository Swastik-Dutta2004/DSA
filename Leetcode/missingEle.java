package Leetcode;

public class missingEle {
    static void missing(int[]arr, int target){
        int lastElement = arr[arr.length - 1] + target;
        int[] normal = {};
        for (int i = 0; i < lastElement; i++) {
            normal[i] = i;
        }

        if (arr[i] == normal[i]) {
            
        }
    }
}
